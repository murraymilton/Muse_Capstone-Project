import { allHotels } from "../../Actions/hotel";

const HotelListings = () => {
  return (
    <div className="container-fluid h1 p-5 text-center">Hotel Listings</div>
  );
};
export default HotelListings;
